// import { useGuildData } from "../../../scripts/client/GuildData";

export default function GuildPanel() {
  // const [guildData] = useGuildData();
  return (
    <div>
      {/*{guildData.map((guild) => {
        
        return <p>{guild.id}</p>;
      })}*/}
    </div>
  );
}
